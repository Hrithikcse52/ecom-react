import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile Page" description="User Mirror">
      <h1>Hello from Profile Page!!</h1>
    </Base>
  );
};

export default Profile;
